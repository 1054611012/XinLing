<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinling.app.mapper.VipPackageMapper">

    <resultMap id="VipPackageResult" type="com.xinling.app.domain.entity.VipPackage">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="description" property="description" />
        <result column="price" property="price" />
        <result column="original_price" property="originalPrice" />
        <result column="days" property="days" />
        <result column="type" property="type" />
        <result column="status" property="status" />
        <result column="sort_order" property="sortOrder" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <sql id="selectColumns">
        id, name, description, price, original_price, days,
        type, status, sort_order, create_time, update_time
    </sql>

    <select id="selectById" resultMap="VipPackageResult">
        SELECT <include refid="selectColumns" />
        FROM vip_package
        WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="VipPackageResult">
        SELECT <include refid="selectColumns" />
        FROM vip_package
        <where>
            <if test="status != null">
                status = #{status}
            </if>
        </where>
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectAll" resultMap="VipPackageResult">
        SELECT <include refid="selectColumns" />
        FROM vip_package
        ORDER BY sort_order ASC, id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO vip_package (
            name, description, price, original_price, days,
            type, status, sort_order, create_time
        ) VALUES (
            #{name}, #{description}, #{price}, #{originalPrice}, #{days},
            #{type}, #{status}, #{sortOrder}, NOW()
        )
    </insert>

    <update id="updateById">
        UPDATE vip_package
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="price != null">price = #{price},</if>
            <if test="originalPrice != null">original_price = #{originalPrice},</if>
            <if test="days != null">days = #{days},</if>
            <if test="type != null">type = #{type},</if>
            <if test="status != null">status = #{status},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
        </set>
        WHERE id = #{id}
    </update>

</mapper>
