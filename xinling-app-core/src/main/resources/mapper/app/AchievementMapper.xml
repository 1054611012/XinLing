<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinling.app.mapper.AchievementMapper">

    <resultMap id="BaseResultMap" type="com.xinling.app.domain.entity.Achievement">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="description" property="description" />
        <result column="icon" property="icon" />
        <result column="condition_type" property="conditionType" />
        <result column="condition_value" property="conditionValue" />
        <result column="points_reward" property="pointsReward" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <sql id="selectColumns">
        id, name, description, icon, condition_type, condition_value, points_reward, create_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="selectColumns" />
        FROM achievement
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="selectColumns" />
        FROM achievement
    </select>

    <select id="selectByConditionType" resultMap="BaseResultMap">
        SELECT <include refid="selectColumns" />
        FROM achievement
        WHERE condition_type = #{conditionType}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO achievement (
            name, description, icon, condition_type, condition_value, points_reward
        ) VALUES (
            #{name}, #{description}, #{icon}, #{conditionType}, #{conditionValue}, #{pointsReward}
        )
    </insert>

</mapper>
