<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinling.app.mapper.SleepRecordMapper">

    <resultMap id="SleepRecordResult" type="com.xinling.app.domain.entity.SleepRecord">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="start_time" property="startTime" />
        <result column="end_time" property="endTime" />
        <result column="duration" property="duration" />
        <result column="sleep_score" property="sleepScore" />
        <result column="deep_sleep_minutes" property="deepSleepMinutes" />
        <result column="light_sleep_minutes" property="lightSleepMinutes" />
        <result column="rem_sleep_minutes" property="remSleepMinutes" />
        <result column="interrupt_count" property="interruptCount" />
        <result column="snoring_count" property="snoringCount" />
        <result column="audio_mix_id" property="audioMixId" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <sql id="selectColumns">
        id, user_id, start_time, end_time, duration, sleep_score,
        deep_sleep_minutes, light_sleep_minutes, rem_sleep_minutes,
        interrupt_count, snoring_count, audio_mix_id,
        create_time, update_time
    </sql>

    <select id="selectById" resultMap="SleepRecordResult">
        SELECT <include refid="selectColumns" />
        FROM sleep_record
        WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="SleepRecordResult">
        SELECT <include refid="selectColumns" />
        FROM sleep_record
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <select id="selectCurrentByUserId" resultMap="SleepRecordResult">
        SELECT <include refid="selectColumns" />
        FROM sleep_record
        WHERE user_id = #{userId}
          AND end_time IS NULL
        ORDER BY create_time DESC
        LIMIT 1
    </select>

    <select id="selectByUserIdAndDate" resultMap="SleepRecordResult">
        SELECT <include refid="selectColumns" />
        FROM sleep_record
        WHERE user_id = #{userId}
          AND DATE(start_time) = DATE(#{date})
        ORDER BY start_time DESC
        LIMIT 1
    </select>

    <select id="selectList" resultMap="SleepRecordResult">
        SELECT <include refid="selectColumns" />
        FROM sleep_record
        <where>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="beginTime != null and beginTime != ''">
                AND create_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND create_time &lt;= #{endTime}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectTodayTotalMinutes" resultType="int">
        SELECT COALESCE(SUM(duration), 0)
        FROM sleep_record
        WHERE user_id = #{userId}
          AND DATE(start_time) = CURDATE()
          AND end_time IS NOT NULL
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sleep_record (
            user_id, start_time, end_time, duration, sleep_score,
            deep_sleep_minutes, light_sleep_minutes, rem_sleep_minutes,
            interrupt_count, snoring_count, audio_mix_id,
            create_time
        ) VALUES (
            #{userId}, #{startTime}, #{endTime}, #{duration}, #{sleepScore},
            #{deepSleepMinutes}, #{lightSleepMinutes}, #{remSleepMinutes},
            #{interruptCount}, #{snoringCount}, #{audioMixId},
            NOW()
        )
    </insert>

    <update id="updateById">
        UPDATE sleep_record
        <set>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="duration != null">duration = #{duration},</if>
            <if test="sleepScore != null">sleep_score = #{sleepScore},</if>
            <if test="deepSleepMinutes != null">deep_sleep_minutes = #{deepSleepMinutes},</if>
            <if test="lightSleepMinutes != null">light_sleep_minutes = #{lightSleepMinutes},</if>
            <if test="remSleepMinutes != null">rem_sleep_minutes = #{remSleepMinutes},</if>
            <if test="interruptCount != null">interrupt_count = #{interruptCount},</if>
            <if test="snoringCount != null">snoring_count = #{snoringCount},</if>
            <if test="audioMixId != null">audio_mix_id = #{audioMixId},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sleep_record
        WHERE id = #{id}
    </delete>

</mapper>
