<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinling.app.mapper.UserCouponMapper">

    <resultMap id="UserCouponResult" type="com.xinling.app.domain.entity.UserCoupon">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="coupon_id" property="couponId" />
        <result column="order_no" property="orderNo" />
        <result column="get_time" property="getTime" />
        <result column="use_time" property="useTime" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <sql id="selectColumns">
        id, user_id, coupon_id, order_no, get_time,
        use_time, status, create_time
    </sql>

    <select id="selectById" resultMap="UserCouponResult">
        SELECT <include refid="selectColumns" />
        FROM user_coupon
        WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="UserCouponResult">
        SELECT <include refid="selectColumns" />
        FROM user_coupon
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByUserIdAndCouponId" resultMap="UserCouponResult">
        SELECT <include refid="selectColumns" />
        FROM user_coupon
        WHERE user_id = #{userId} AND coupon_id = #{couponId}
        ORDER BY create_time DESC
        LIMIT 1
    </select>

    <select id="selectUsableByUserId" resultMap="UserCouponResult">
        SELECT <include refid="selectColumns" />
        FROM user_coupon
        WHERE user_id = #{userId} AND status = 0
        ORDER BY create_time DESC
    </select>

    <select id="countByUserIdAndCouponId" resultType="int">
        SELECT COUNT(*)
        FROM user_coupon
        WHERE user_id = #{userId} AND coupon_id = #{couponId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_coupon (
            user_id, coupon_id, order_no, get_time, use_time,
            status, create_time
        ) VALUES (
            #{userId}, #{couponId}, #{orderNo}, #{getTime}, #{useTime},
            #{status}, NOW()
        )
    </insert>

    <update id="updateById">
        UPDATE user_coupon
        <set>
            <if test="orderNo != null">order_no = #{orderNo},</if>
            <if test="useTime != null">use_time = #{useTime},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="markUsed">
        UPDATE user_coupon
        SET status = 1, order_no = #{orderNo}, use_time = NOW()
        WHERE id = #{id} AND status = 0
    </update>

    <update id="markExpired">
        UPDATE user_coupon
        SET status = 2
        WHERE id = #{id} AND status = 0
    </update>

</mapper>
