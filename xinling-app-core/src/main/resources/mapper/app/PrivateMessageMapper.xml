<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinling.app.mapper.PrivateMessageMapper">

    <resultMap id="BaseResultMap" type="com.xinling.app.domain.entity.PrivateMessage">
        <id column="id" property="id" />
        <result column="from_user_id" property="fromUserId" />
        <result column="to_user_id" property="toUserId" />
        <result column="content" property="content" />
        <result column="is_read" property="isRead" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <sql id="selectColumns">
        id, from_user_id, to_user_id, content, is_read, create_time
    </sql>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="selectColumns" />
        FROM private_message
        WHERE from_user_id = #{userId} OR to_user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO private_message (from_user_id, to_user_id, content, create_time)
        VALUES (#{fromUserId}, #{toUserId}, #{content}, NOW())
    </insert>

    <update id="markAsRead">
        UPDATE private_message
        SET is_read = 1
        WHERE to_user_id = #{userId} AND from_user_id = #{fromUserId} AND is_read = 0
    </update>

</mapper>
