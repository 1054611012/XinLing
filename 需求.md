# AI 沉浸式专注助眠 APP 完整需求文档（V3.2 终极完整版）
**适用范围**：Vue3 移动端（H5/PWA）、SpringBoot 后端、Vue3 管理后台  
**文档版本**：V3.2  
**更新日期**：2026-05-28  
**核心定位**：以 AI 情绪陪伴为核心差异化，打造沉浸式专注与助眠体验的移动应用  
**成功关键**：沉浸感 > 高级感 > 情绪价值 > AI 陪伴感 > 社区温度 > 商业变现能力

---

## 一、项目概述
### 1.1 产品定位
一款集**专注计时、白噪音混音、智能助眠、AI 情绪陪伴、轻量社区、分销裂变、活动运营**于一体的沉浸式健康应用，通过极简设计、慢节奏动效和个性化 AI 服务，帮助用户提升专注力、改善睡眠质量、缓解情绪压力，同时构建一个安静温暖的互助成长社区和可持续的商业生态。

### 1.2 目标用户
- 学生群体：需要专注学习、备考的中小学生和大学生
- 职场人士：需要提高工作效率、缓解工作压力的上班族
- 睡眠障碍人群：入睡困难、睡眠质量差的用户
- 情绪敏感人群：需要情绪陪伴和心理疏导的用户
- 自律爱好者：追求自我提升、习惯养成的用户
- 副业需求者：希望通过分享获得额外收入的用户

### 1.3 核心价值
- **专注**：通过番茄钟、严格锁机、白噪音打造无干扰专注环境
- **助眠**：通过智能睡眠监测、个性化助眠音、AI 睡前陪伴改善睡眠
- **情绪**：通过 AI 聊天、情绪日记、呼吸引导提供情绪价值
- **成长**：通过数据统计、成就系统、成长等级记录用户进步
- **连接**：通过轻量社区实现同好互助、互相监督、分享进步
- **共赢**：通过分销系统让用户与平台共享增长收益
- **运营**：通过活动系统提升用户转化和留存

---

## 二、整体架构
### 2.1 项目结构
```text
project
├── admin-web              # 管理后台 (Vue3 + ElementPlus)
├── mobile-web             # 移动端 (Vue3 + Vite + Vant)
│
├── springboot-server      # 后端服务 (SpringBoot3 + MyBatis Plus)
│   ├── user               # 用户模块
│   ├── focus              # 专注模块
│   ├── audio              # 音频模块
│   ├── sleep              # 睡眠模块
│   ├── ai                 # AI 模块
│   ├── vip                # 会员模块
│   ├── order              # 订单模块
│   ├── pay                # 支付模块
│   ├── distribution       # 分销模块
│   ├── activity           # 活动通知模块
│   ├── whitelist          # 白名单模块
│   ├── statistics         # 统计模块
│   ├── growth             # 成长模块
│   ├── emotion            # 情绪模块
│   ├── moment             # 动态社区模块
│   └── notification       # 通知消息模块
│
├── mysql                  # 关系型数据库
├── redis                  # 缓存与分布式锁
└── minio                  # 对象存储
```

### 2.2 技术栈
#### 前端
| 模块 | 技术栈 |
|------|--------|
| 管理后台 | Vue3 + ElementPlus + Pinia + Axios + Echarts |
| 移动端 | Vue3 + Vite + Vant + Pinia + Axios + Vue-Router + GSAP + Howler.js |

#### 后端
```text
SpringBoot3
MyBatis Plus
Redis
JWT + Sa-Token
MinIO
Knife4j
Spring AI + LangChain4j
RabbitMQ (异步任务)
WebSocket (实时消息)
```

#### AI
- 大模型：通义千问 Qwen、DeepSeek
- 语音合成：阿里云 TTS、腾讯云 TTS
- 语音识别：阿里云 ASR

---

## 三、系统模块详细设计
### 3.1 用户系统
#### 3.1.1 核心功能
- **登录注册**：手机号验证码登录、邮箱登录、微信/QQ/Apple 第三方登录
- **认证授权**：JWT 认证、自动登录、Token 续期、多设备登录管理
- **个人信息**：昵称、头像、性别、生日、个人简介
- **偏好设置**：深色模式、通知开关、默认专注时长、默认音量
- **账号安全**：修改密码、绑定手机/邮箱、远程登出、账号注销
- **数据管理**：数据导出、数据删除、本地备份与恢复
- **分销关系**：自动绑定上下级关系，查看我的邀请人

#### 3.1.2 接口设计
```http
# 登录注册
POST /api/app/user/sendCode
POST /api/app/user/login
POST /api/app/user/thirdLogin
POST /api/app/user/refreshToken
POST /api/app/user/logout

# 用户信息
GET  /api/app/user/info
POST /api/app/user/update
POST /api/app/user/updateAvatar

# 账号安全
GET  /api/app/user/devices
POST /api/app/user/logoutDevice/{deviceId}
POST /api/app/user/deleteAccount

# 数据管理
GET  /api/app/user/exportData
POST /api/app/user/restoreData

# 分销关系
GET  /api/app/user/inviter
```

#### 3.1.3 数据表设计
```sql
-- 用户表
CREATE TABLE user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    nickname VARCHAR(50) COMMENT '昵称',
    avatar VARCHAR(255) COMMENT '头像URL',
    phone VARCHAR(20) UNIQUE COMMENT '手机号',
    email VARCHAR(50) UNIQUE COMMENT '邮箱',
    password_hash VARCHAR(255) COMMENT '密码哈希',
    gender TINYINT COMMENT '性别 0-未知 1-男 2-女',
    birthday DATE COMMENT '生日',
    vip_status TINYINT DEFAULT 0 COMMENT 'VIP状态 0-普通 1-VIP 2-终身VIP',
    focus_days INT DEFAULT 0 COMMENT '连续专注天数',
    total_focus_minutes INT DEFAULT 0 COMMENT '总专注分钟数',
    total_sleep_minutes INT DEFAULT 0 COMMENT '总睡眠分钟数',
    inviter_id BIGINT DEFAULT 0 COMMENT '邀请人ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    is_deleted TINYINT DEFAULT 0 COMMENT '逻辑删除',
    version INT DEFAULT 0 COMMENT '乐观锁版本号',
    INDEX idx_inviter_id (inviter_id)
);

-- 用户设置表
CREATE TABLE user_settings (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT UNIQUE COMMENT '用户ID',
    default_focus_time INT DEFAULT 25 COMMENT '默认专注时长(分钟)',
    default_break_time INT DEFAULT 5 COMMENT '默认休息时长(分钟)',
    default_audio_id BIGINT COMMENT '默认音频ID',
    dark_mode TINYINT DEFAULT 1 COMMENT '深色模式 0-关闭 1-开启 2-跟随系统',
    notification TINYINT DEFAULT 1 COMMENT '通知开关',
    volume INT DEFAULT 70 COMMENT '默认音量(0-100)',
    ai_voice_id VARCHAR(50) COMMENT '默认AI语音ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 登录设备表
CREATE TABLE user_device (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    device_id VARCHAR(100) COMMENT '设备唯一标识',
    device_name VARCHAR(100) COMMENT '设备名称',
    device_type VARCHAR(20) COMMENT '设备类型 Android/iOS/Web',
    login_time DATETIME COMMENT '登录时间',
    last_active_time DATETIME COMMENT '最后活跃时间',
    ip_address VARCHAR(50) COMMENT 'IP地址',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id)
);
```

### 3.2 首页模块
#### 3.2.1 核心功能
- **动态背景**：根据时间、天气、用户状态自动切换渐变背景
- **AI 问候**：根据时间和用户使用习惯生成个性化问候语
- **快速入口**：一键开始专注、一键开始睡眠
- **今日概览**：今日专注时长、今日睡眠时长、连续打卡天数
- **天气信息**：当前城市天气、温度、空气质量
- **推荐内容**：根据用户偏好推荐音频和AI话题
- **社区入口**：热门动态、好友动态预览
- **活动入口**：热门活动弹窗、Banner展示
- **分销入口**：分销员专属入口，显示今日收益

#### 3.2.2 接口设计
```http
GET /api/app/home/info
```

#### 3.2.3 返回示例
```json
{
  "greeting": "晚上好，今天辛苦了",
  "todayFocusMinutes": 120,
  "todaySleepMinutes": 420,
  "continuousDays": 7,
  "weather": {
    "city": "深圳",
    "weather": "小雨",
    "temperature": 25,
    "aqi": 45
  },
  "recommendedAudio": {
    "id": 1,
    "title": "森林雨声",
    "cover": "https://xxx.com/cover.jpg"
  },
  "aiTopic": "今天有什么开心的事想分享吗？",
  "hotMoments": [
    {
      "id": 1001,
      "userNickname": "小专注",
      "content": "今天完成了3小时的学习，继续加油！",
      "likeCount": 28
    }
  ],
  "hotActivity": {
    "id": 101,
    "title": "世界睡眠日特惠",
    "cover": "https://xxx.com/activity.jpg"
  },
  "distributionInfo": {
    "isDistributor": true,
    "todayCommission": 15.60,
    "totalCommission": 238.50
  }
}
```

### 3.3 专注模块（核心）
#### 3.3.1 核心功能
- **专注模式选择**：
   - 番茄模式：25分钟专注 + 5分钟休息
   - 深度工作模式：90分钟专注 + 15分钟休息
   - 自由模式：自定义专注时长
- **深度专注锁**：
   - 开启后禁止退出APP
   - 系统级勿扰模式
   - 分心APP屏蔽（Android无障碍权限/iOS屏幕时间API）
   - 白名单应用设置
   - 分心检测与提醒
- **专注过程**：
   - 圆形倒计时动画
   - 呼吸引导动画
   - 白噪音播放
   - 暂停/继续/结束
   - AI 定时鼓励
- **专注记录**：
   - 专注时长、开始/结束时间
   - 中断次数、中断原因
   - 专注标签（学习、工作、阅读等）
   - 专注笔记
- **休息提醒**：
   - 休息倒计时
   - 拉伸提示
   - 护眼提醒
   - 喝水提醒

#### 3.3.2 接口设计
```http
# 专注操作
POST /api/app/focus/start
POST /api/app/focus/pause
POST /api/app/focus/resume
POST /api/app/focus/end
POST /api/app/focus/interrupt

# 专注记录
GET  /api/app/focus/records
GET  /api/app/focus/record/{id}
POST /api/app/focus/record/update

# 专注设置
GET  /api/app/focus/settings
POST /api/app/focus/settings/update
```

#### 3.3.3 数据表设计
```sql
-- 专注记录表
CREATE TABLE focus_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    start_time DATETIME COMMENT '开始时间',
    end_time DATETIME COMMENT '结束时间',
    duration INT COMMENT '专注时长(分钟)',
    status TINYINT COMMENT '状态 0-进行中 1-已完成 2-已中断',
    mode VARCHAR(20) COMMENT '模式 tomato/deep/free',
    tag VARCHAR(50) COMMENT '专注标签',
    interrupt_count INT DEFAULT 0 COMMENT '中断次数',
    note TEXT COMMENT '专注笔记',
    audio_mix_id BIGINT COMMENT '使用的混音ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    is_deleted TINYINT DEFAULT 0,
    INDEX idx_user_id (user_id),
    INDEX idx_create_time (create_time)
);

-- 专注设置表
CREATE TABLE focus_settings (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT UNIQUE COMMENT '用户ID',
    strict_mode TINYINT DEFAULT 0 COMMENT '严格模式 0-关闭 1-开启',
    app_block TINYINT DEFAULT 0 COMMENT 'APP屏蔽',
    allowed_apps JSON COMMENT '白名单应用',
    notification_block TINYINT DEFAULT 1 COMMENT '通知屏蔽',
    ai_encouragement TINYINT DEFAULT 1 COMMENT 'AI鼓励',
    encouragement_interval INT DEFAULT 30 COMMENT '鼓励间隔(分钟)',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 3.4 白噪音模块（核心）
#### 3.4.1 核心功能
- **音频分类**：雨声、海浪、森林、夜晚、咖啡馆、火车、冥想、ASMR
- **混合音频引擎**：
   - 支持同时播放3-5种音频
   - 每种音频音量独立调节
   - 官方预设混音包
   - 用户自定义混音保存
   - 混音分享
- **音频播放能力**：
   - 后台播放
   - 锁屏播放控制
   - 淡入淡出效果
   - 定时关闭
   - 本地缓存与离线播放
   - 音频中断自动恢复
- **音频管理**：
   - 收藏音频
   - 下载音频
   - 播放历史
   - 搜索音频

#### 3.4.2 接口设计
```http
# 音频管理
GET  /api/app/audio/list
GET  /api/app/audio/detail/{id}
GET  /api/app/audio/search

# 混音管理
GET  /api/app/audio/mix/list
POST /api/app/audio/mix/save
POST /api/app/audio/mix/delete/{id}
POST /api/app/audio/mix/share/{id}

# 播放记录
GET  /api/app/audio/history
POST /api/app/audio/collect/{id}
POST /api/app/audio/uncollect/{id}
```

#### 3.4.3 数据表设计
```sql
-- 音频表
CREATE TABLE audio (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(100) COMMENT '标题',
    description VARCHAR(255) COMMENT '描述',
    cover VARCHAR(255) COMMENT '封面URL',
    audio_url VARCHAR(255) COMMENT '音频URL',
    duration INT COMMENT '时长(秒)',
    type VARCHAR(20) COMMENT '分类',
    vip_only TINYINT DEFAULT 0 COMMENT '是否VIP专属',
    play_count INT DEFAULT 0 COMMENT '播放次数',
    status TINYINT DEFAULT 1 COMMENT '状态 0-下架 1-上架',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    is_deleted TINYINT DEFAULT 0,
    INDEX idx_type (type)
);

-- 混音表
CREATE TABLE audio_mix (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    name VARCHAR(50) COMMENT '混音名称',
    cover VARCHAR(255) COMMENT '封面URL',
    mix_data JSON COMMENT '混音数据 [{"audioId":1,"volume":70}, ...]',
    is_public TINYINT DEFAULT 0 COMMENT '是否公开',
    play_count INT DEFAULT 0 COMMENT '播放次数',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    is_deleted TINYINT DEFAULT 0,
    INDEX idx_user_id (user_id)
);

-- 用户收藏表
CREATE TABLE user_collect (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    type VARCHAR(20) COMMENT '类型 audio/mix',
    target_id BIGINT COMMENT '目标ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_target (user_id, type, target_id)
);
```

### 3.5 睡眠模块（核心）
#### 3.5.1 核心功能
- **助眠音库**：专业助眠音乐、白噪音、ASMR、引导式冥想
- **智能睡眠监测**：
   - 通过麦克风监测呼吸和翻身
   - 识别浅睡/深睡/REM睡眠阶段
   - 睡眠中断检测
   - 打鼾检测
- **智能唤醒**：
   - 在设定的时间窗口内选择浅睡眠阶段唤醒
   - 渐进式音量增大
   - 配合智能灯光渐变
- **睡眠记录**：
   - 入睡时间、起床时间
   - 睡眠时长、睡眠效率
   - 睡眠阶段分布
   - 睡眠评分
- **睡眠日记**：
   - 记录睡前活动、咖啡因摄入、情绪状态
   - 分析影响睡眠质量的因素
   - 生成睡眠改善建议

#### 3.5.2 接口设计
```http
# 睡眠操作
POST /api/app/sleep/start
POST /api/app/sleep/end
POST /api/app/sleep/setWakeUp

# 睡眠记录
GET  /api/app/sleep/records
GET  /api/app/sleep/record/{id}
GET  /api/app/sleep/report/{date}

# 睡眠设置
GET  /api/app/sleep/settings
POST /api/app/sleep/settings/update
```

#### 3.5.3 数据表设计
```sql
-- 睡眠记录表
CREATE TABLE sleep_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    start_time DATETIME COMMENT '入睡时间',
    end_time DATETIME COMMENT '起床时间',
    duration INT COMMENT '睡眠时长(分钟)',
    sleep_score INT COMMENT '睡眠评分(0-100)',
    deep_sleep_minutes INT COMMENT '深睡时长(分钟)',
    light_sleep_minutes INT COMMENT '浅睡时长(分钟)',
    rem_sleep_minutes INT COMMENT 'REM睡眠时长(分钟)',
    interrupt_count INT DEFAULT 0 COMMENT '中断次数',
    snoring_count INT DEFAULT 0 COMMENT '打鼾次数',
    audio_mix_id BIGINT COMMENT '使用的助眠音ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    is_deleted TINYINT DEFAULT 0,
    INDEX idx_user_id (user_id),
    INDEX idx_create_time (create_time)
);

-- 睡眠日记表
CREATE TABLE sleep_diary (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    date DATE COMMENT '日期',
    bedtime_activity VARCHAR(255) COMMENT '睡前活动',
    caffeine_intake TINYINT COMMENT '咖啡因摄入 0-无 1-少量 2-大量',
    exercise TINYINT COMMENT '运动情况 0-无 1-轻度 2-中度 3-重度',
    emotion VARCHAR(20) COMMENT '情绪状态',
    note TEXT COMMENT '备注',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_date (user_id, date)
);
```

### 3.6 AI 陪伴模块（核心差异化）
#### 3.6.1 核心功能
- **多场景 AI 聊天**：
   - 情绪安慰：倾听用户烦恼，提供心理支持
   - 睡前陪伴：睡前聊天、讲睡前故事
   - 专注鼓励：专注过程中定时发送鼓励消息
   - 生活建议：提供学习、工作、生活方面的建议
- **AI 主动交互**：
   - 定时问候：早上起床、午休、晚上睡前主动问候
   - 状态感知：根据用户使用状态主动提供帮助
   - 情绪记忆：记住用户之前提到的事情
   - 习惯提醒：提醒用户按时专注、按时睡觉
   - 活动推荐：AI聊天时主动推荐合适的活动
- **多模态 AI 能力**：
   - 语音输入：支持语音转文字
   - 语音合成：AI 回复支持语音播放，多种音色可选
   - 情绪语音：根据内容调整语音语气
   - AI 生成音频：根据用户描述生成个性化白噪音
- **情绪健康系统**：
   - 情绪日记：AI 引导用户记录每日情绪
   - 情绪分析：分析用户情绪变化趋势
   - 情绪干预：检测到负面情绪时主动提供安慰
   - 呼吸引导：引导用户进行深呼吸练习

#### 3.6.2 接口设计
```http
# 聊天接口
POST /api/app/chat/send
POST /api/app/chat/sendStream  # 流式响应
POST /api/app/chat/voiceSend   # 语音输入

# 聊天记录
GET  /api/app/chat/history
POST /api/app/chat/clearHistory

# 情绪记录
GET  /api/app/emotion/records
POST /api/app/emotion/record
GET  /api/app/emotion/report

# AI 设置
GET  /api/app/ai/settings
POST /api/app/ai/settings/update
```

#### 3.6.3 数据表设计
```sql
-- AI 聊天记录表
CREATE TABLE ai_chat_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    session_id VARCHAR(50) COMMENT '会话ID',
    role VARCHAR(20) COMMENT '角色 user/assistant',
    content TEXT COMMENT '内容',
    voice_url VARCHAR(255) COMMENT '语音URL',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    is_deleted TINYINT DEFAULT 0,
    INDEX idx_user_id (user_id),
    INDEX idx_session_id (session_id)
);

-- 情绪记录表
CREATE TABLE emotion_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    emotion VARCHAR(20) COMMENT '情绪类型 happy/calm/anxious/sad/angry',
    score INT COMMENT '情绪评分(1-10)',
    note TEXT COMMENT '备注',
    source VARCHAR(20) COMMENT '来源 user/ai',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id)
);

-- AI 语音表
CREATE TABLE ai_voice (
    id VARCHAR(50) PRIMARY KEY COMMENT '语音ID',
    name VARCHAR(50) COMMENT '语音名称',
    description VARCHAR(255) COMMENT '描述',
    avatar VARCHAR(255) COMMENT '头像',
    vip_only TINYINT DEFAULT 0 COMMENT '是否VIP专属',
    status TINYINT DEFAULT 1 COMMENT '状态',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 3.7 会员系统（核心盈利）
#### 3.7.1 会员等级与权益
| 功能 | 免费用户 | 月度VIP | 年度VIP | 终身VIP | 分销员 |
|------|----------|---------|---------|---------|--------|
| AI聊天次数 | 每日5次 | 无限 | 无限 | 无限 | 无限 |
| AI语音合成 | 每日3次 | 无限 | 无限 | 无限 | 无限 |
| 白噪音数量 | 基础20种 | 全部100+种 | 全部100+种 | 全部100+种 | 全部100+种 |
| 混音数量 | 最多2种 | 最多5种 | 最多5种 | 最多5种 | 最多5种 |
| 自定义混音 | 最多3个 | 无限 | 无限 | 无限 | 无限 |
| 睡眠监测 | 基础 | 高级 | 高级 | 高级 | 高级 |
| 智能唤醒 | ❌ | ✅ | ✅ | ✅ | ✅ |
| 数据报告 | 基础 | 详细 | 详细 | 详细 | 详细 |
| 专属主题 | ❌ | ✅ | ✅ | ✅ | ✅ |
| 专属AI语音 | ❌ | 部分 | 全部 | 全部 | 全部 |
| 数据导出 | ❌ | ✅ | ✅ | ✅ | ✅ |
| 私信功能 | ❌ | ✅ | ✅ | ✅ | ✅ |
| 完整排行榜 | 前100名 | 全部 | 全部 | 全部 | 全部 |
| 专属挑战活动 | ❌ | ✅ | ✅ | ✅ | ✅ |
| 动态置顶 | ❌ | ✅ | ✅ | ✅ | ✅ |
| 优先客服 | ❌ | ❌ | ✅ | ✅ | ✅ |
| 永久更新 | ❌ | ❌ | ❌ | ✅ | ✅ |
| 自己购买折扣 | 无 | 无 | 无 | 无 | 8折 |
| 推广佣金 | ❌ | ❌ | ❌ | ❌ | ✅ |

#### 3.7.2 核心功能
- **会员套餐管理**：月度、季度、年度、终身套餐
- **免费试用**：新用户7天免费试用VIP
- **活动优惠**：支持限时折扣、优惠券、买一送一等活动
- **自动续费**：支持支付宝、微信、Apple Pay自动续费及取消
- **订单管理**：订单查询、退款申请
- **会员权益展示**：清晰展示各级别会员权益

#### 3.7.3 接口设计
```http
# 会员套餐
GET  /api/app/vip/package/list
GET  /api/app/vip/activity/list

# 会员信息
GET  /api/app/user/vipInfo
POST /api/app/vip/cancelAutoRenew
```

#### 3.7.4 数据表设计
```sql
-- 会员套餐表
CREATE TABLE vip_package (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) COMMENT '套餐名称',
    description VARCHAR(255) COMMENT '套餐描述',
    price DECIMAL(10,2) COMMENT '价格',
    original_price DECIMAL(10,2) COMMENT '原价',
    days INT COMMENT '天数',
    type VARCHAR(20) COMMENT '类型 month/quarter/year/lifetime',
    status TINYINT DEFAULT 1 COMMENT '状态',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 用户会员表
CREATE TABLE user_vip (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT UNIQUE COMMENT '用户ID',
    package_id BIGINT COMMENT '套餐ID',
    package_name VARCHAR(50) COMMENT '套餐名称',
    start_time DATETIME COMMENT '开始时间',
    end_time DATETIME COMMENT '结束时间',
    auto_renew TINYINT DEFAULT 0 COMMENT '自动续费',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 3.8 订单系统（核心盈利）
#### 3.8.1 核心功能
- **用户端订单管理**：
   - 全部订单、待支付、已支付、已取消、已退款分类筛选
   - 订单状态实时同步
   - 订单详情查看（包含商品信息、支付信息、分销信息、优惠信息）
   - 待支付订单取消（15分钟自动取消）
   - 已支付订单申请退款
   - 订单支付凭证下载
- **管理后台订单管理**：
   - 全量订单列表，支持多条件筛选（订单号、用户ID、支付方式、时间范围、状态）
   - 订单详情查看（包含用户信息、支付流水、分销佣金明细）
   - 手动取消订单
   - 退款审核与处理
   - 订单导出（Excel格式）
   - 订单统计分析（每日/每周/每月订单量、收入、退款率）

#### 3.8.2 接口设计
```http
# 用户端订单接口
GET  /api/app/order/list?status=all&page=1&size=10
GET  /api/app/order/detail/{orderNo}
POST /api/app/order/cancel/{orderNo}
POST /api/app/order/applyRefund/{orderNo}
GET  /api/app/order/refundRecord/{orderNo}

# 管理后台订单接口
GET  /api/admin/order/list
GET  /api/admin/order/detail/{orderNo}
POST /api/admin/order/cancel/{orderNo}
POST /api/admin/order/auditRefund/{orderNo}
POST /api/admin/order/export
GET  /api/admin/order/statistics
```

#### 3.8.3 数据表设计
```sql
-- 支付订单表
CREATE TABLE pay_order (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_no VARCHAR(50) UNIQUE COMMENT '订单号',
    user_id BIGINT COMMENT '用户ID',
    package_id BIGINT COMMENT '套餐ID',
    package_name VARCHAR(50) COMMENT '套餐名称',
    amount DECIMAL(10,2) COMMENT '订单金额',
    pay_amount DECIMAL(10,2) COMMENT '实际支付金额',
    discount_amount DECIMAL(10,2) DEFAULT 0 COMMENT '优惠金额',
    coupon_id BIGINT DEFAULT 0 COMMENT '使用的优惠券ID',
    pay_type VARCHAR(20) COMMENT '支付方式 alipay/wechat/apple',
    order_status TINYINT COMMENT '状态 0-待支付 1-已支付 2-已取消 3-已退款 4-已过期',
    pay_time DATETIME COMMENT '支付时间',
    expire_time DATETIME COMMENT '订单过期时间',
    transaction_id VARCHAR(50) COMMENT '第三方交易号',
    refund_amount DECIMAL(10,2) DEFAULT 0 COMMENT '退款金额',
    refund_time DATETIME COMMENT '退款时间',
    refund_reason TEXT COMMENT '退款原因',
    distributor_id BIGINT DEFAULT 0 COMMENT '分销员ID',
    commission_amount DECIMAL(10,2) DEFAULT 0 COMMENT '分销佣金金额',
    activity_id BIGINT DEFAULT 0 COMMENT '参与的活动ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    is_deleted TINYINT DEFAULT 0,
    INDEX idx_user_id (user_id),
    INDEX idx_order_status (order_status),
    INDEX idx_create_time (create_time),
    INDEX idx_distributor_id (distributor_id)
);

-- 退款记录表
CREATE TABLE order_refund (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_no VARCHAR(50) COMMENT '订单号',
    user_id BIGINT COMMENT '用户ID',
    refund_amount DECIMAL(10,2) COMMENT '退款金额',
    refund_reason TEXT COMMENT '退款原因',
    refund_status TINYINT COMMENT '状态 0-待审核 1-审核通过 2-审核拒绝 3-退款中 4-退款完成',
    audit_user_id BIGINT COMMENT '审核人ID',
    audit_time DATETIME COMMENT '审核时间',
    audit_remark TEXT COMMENT '审核备注',
    transaction_id VARCHAR(50) COMMENT '退款交易号',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_order_no (order_no)
);
```

### 3.9 支付系统（核心盈利）
#### 3.9.1 设计原则
- **安全第一**：全程加密传输，防篡改、防重放、防钓鱼
- **高可用**：多支付渠道备份，支付失败自动降级
- **用户体验**：支付流程简洁，支持一键支付
- **可扩展**：易于接入新的支付方式和第三方平台

#### 3.9.2 核心功能
- **支付方式支持**：
   - 主流支付：支付宝、微信支付、Apple Pay
   - 第三方支付：QQ钱包、银联支付
   - 虚拟支付：积分抵扣、优惠券抵扣
   - 组合支付：支持"积分+现金"组合支付
- **支付流程**：
   - 创建订单：生成唯一订单号，计算订单金额和优惠
   - 发起支付：调用第三方支付接口，生成支付链接/二维码
   - 支付回调：接收第三方支付结果通知，更新订单状态
   - 主动查询：支付回调失败时，主动查询订单状态
   - 订单完成：支付成功后，自动开通会员权益，计算分销佣金
- **自动续费管理**：
   - 自动续费开通：用户开通连续包月/包季/包年
   - 自动扣费：会员到期前3天自动扣费
   - 续费提醒：扣费前3天、1天分别发送提醒通知
   - 自动续费取消：用户可随时取消自动续费
   - 续费失败处理：扣费失败时重试3次，仍失败则发送通知
- **支付安全**：
   - 签名验证：所有支付请求和回调都进行签名验证
   - 防重放攻击：使用时间戳和随机数防止重放攻击
   - 订单幂等性：保证同一订单不会被重复支付
   - 敏感信息加密：支付密码、银行卡号等敏感信息加密存储
   - 风险控制：异常支付行为检测和拦截
- **退款管理**：
   - 退款申请：用户提交退款申请
   - 退款审核：管理后台审核退款申请
   - 退款执行：调用第三方支付接口执行退款
   - 退款状态同步：实时同步退款状态
   - 佣金回退：退款成功后，自动回退已结算的分销佣金

#### 3.9.3 接口设计
```http
# 支付接口
POST /api/app/pay/createOrder
POST /api/app/pay/getPayUrl
POST /api/app/pay/queryOrderStatus/{orderNo}

# 自动续费接口
POST /api/app/pay/openAutoRenew
POST /api/app/pay/closeAutoRenew
GET  /api/app/pay/autoRenewStatus

# 退款接口
POST /api/app/pay/applyRefund
GET  /api/app/pay/refundStatus/{refundId}

# 回调接口（第三方调用）
POST /api/app/pay/callback/alipay
POST /api/app/pay/callback/wechat
POST /api/app/pay/callback/apple

# 管理后台支付接口
GET  /api/admin/pay/config
POST /api/admin/pay/config/update
GET  /api/admin/pay/transaction/list
POST /api/admin/pay/refund/audit
```

#### 3.9.4 数据表设计
```sql
-- 支付配置表
CREATE TABLE pay_config (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    pay_type VARCHAR(20) COMMENT '支付方式 alipay/wechat/apple',
    app_id VARCHAR(100) COMMENT '应用ID',
    merchant_id VARCHAR(100) COMMENT '商户号',
    private_key TEXT COMMENT '私钥',
    public_key TEXT COMMENT '公钥',
    notify_url VARCHAR(255) COMMENT '回调地址',
    status TINYINT DEFAULT 1 COMMENT '状态 0-关闭 1-开启',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 交易记录表
CREATE TABLE pay_transaction (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_no VARCHAR(50) COMMENT '订单号',
    transaction_id VARCHAR(50) COMMENT '第三方交易号',
    pay_type VARCHAR(20) COMMENT '支付方式',
    amount DECIMAL(10,2) COMMENT '交易金额',
    status TINYINT COMMENT '状态 0-待支付 1-支付成功 2-支付失败 3-已退款',
    pay_time DATETIME COMMENT '支付时间',
    raw_data TEXT COMMENT '原始支付数据',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_order_no (order_no),
    INDEX idx_transaction_id (transaction_id)
);

-- 自动续费表
CREATE TABLE auto_renew (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    package_id BIGINT COMMENT '套餐ID',
    pay_type VARCHAR(20) COMMENT '支付方式',
    agreement_id VARCHAR(100) COMMENT '代扣协议ID',
    next_pay_time DATETIME COMMENT '下次扣费时间',
    status TINYINT DEFAULT 1 COMMENT '状态 0-已关闭 1-已开启',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_package (user_id, package_id)
);

-- 优惠券表
CREATE TABLE coupon (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) COMMENT '优惠券名称',
    type VARCHAR(20) COMMENT '类型 discount/reduction/full_reduction',
    value DECIMAL(10,2) COMMENT '优惠值',
    condition_amount DECIMAL(10,2) COMMENT '满减条件',
    start_time DATETIME COMMENT '开始时间',
    end_time DATETIME COMMENT '结束时间',
    total_count INT COMMENT '总数量',
    used_count INT DEFAULT 0 COMMENT '已使用数量',
    status TINYINT DEFAULT 1 COMMENT '状态',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 用户优惠券表
CREATE TABLE user_coupon (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    coupon_id BIGINT COMMENT '优惠券ID',
    order_no VARCHAR(50) COMMENT '使用的订单号',
    get_time DATETIME COMMENT '领取时间',
    use_time DATETIME COMMENT '使用时间',
    status TINYINT DEFAULT 0 COMMENT '状态 0-未使用 1-已使用 2-已过期',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_coupon_id (coupon_id)
);
```

#### 3.9.5 支付状态流转
```
待支付 → 支付成功 → 订单完成
       ↓
       支付失败 → 订单取消
       ↓
       已过期 → 订单取消

支付成功 → 申请退款 → 退款审核中 → 退款成功
                               ↓
                               退款失败
```

### 3.10 分销系统（核心增长）
#### 3.10.1 设计原则
- **合规优先**：采用**二级分销**模式，严格遵守《禁止传销条例》
- **简单易用**：一键生成推广链接/二维码，自动绑定上下级关系
- **即时激励**：订单支付后立即计算佣金，提现快速到账
- **透明公开**：佣金规则清晰，推广数据实时可查

#### 3.10.2 核心功能
- **分销员体系**：
   - 用户自主申请成为分销员，自动审核通过
   - 分销等级：普通分销员(30%一级/10%二级) → 高级分销员(35%一级/12%二级) → 金牌分销员(40%一级/15%二级+5%团队奖励)
   - 分销员权益：专属推广海报、自己购买8折、专属客服
- **推广关系管理**：
   - 生成专属推广链接和海报（自动包含用户头像和二维码）
   - 用户通过推广链接注册，永久绑定上下级关系
   - 防作弊机制：同一设备/IP只能绑定一个上级
   - 我的团队：查看直接/间接下级列表、团队统计数据
- **佣金管理**：
   - 订单支付成功后立即计算佣金，T+7天结算
   - 订单退款时自动扣除对应佣金
   - 佣金明细：每笔佣金的来源、类型、金额清晰可查
- **提现管理**：
   - 提现方式：支付宝、微信钱包
   - 最低提现金额：10元，手续费1%（最低1元）
   - 提现流程：用户申请 → 自动审核 → 财务打款（1-3个工作日）
- **分销数据统计**：
   - 个人数据：累计推广人数、累计订单数、累计佣金收入
   - 排行榜：分销员佣金排行榜、推广人数排行榜

#### 3.10.3 接口设计
```http
# 分销员管理
POST /api/app/distributor/apply
GET  /api/app/distributor/info
POST /api/app/distributor/updateInfo

# 推广管理
GET  /api/app/distributor/promotionCode
GET  /api/app/distributor/promotionPoster
POST /api/app/distributor/share

# 团队管理
GET  /api/app/distributor/team/direct
GET  /api/app/distributor/team/indirect
GET  /api/app/distributor/team/statistics

# 佣金管理
GET  /api/app/distributor/commission/overview
GET  /api/app/distributor/commission/detail
GET  /api/app/distributor/commission/statistics

# 提现管理
POST /api/app/distributor/withdraw/apply
GET  /api/app/distributor/withdraw/list
GET  /api/app/distributor/withdraw/detail/{id}

# 管理后台分销接口
GET  /api/admin/distributor/list
POST /api/admin/distributor/audit/{id}
GET  /api/admin/distributor/detail/{id}
GET  /api/admin/distribution/order/list
GET  /api/admin/distribution/commission/list
GET  /api/admin/distribution/withdraw/list
POST /api/admin/distribution/withdraw/audit/{id}
POST /api/admin/distribution/settings/update
```

#### 3.10.4 数据表设计
```sql
-- 分销员表
CREATE TABLE distributor (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT UNIQUE COMMENT '用户ID',
    level TINYINT DEFAULT 1 COMMENT '等级 1-普通 2-高级 3-金牌',
    real_name VARCHAR(50) COMMENT '真实姓名',
    id_card VARCHAR(20) COMMENT '身份证号',
    phone VARCHAR(20) COMMENT '手机号',
    alipay_account VARCHAR(50) COMMENT '支付宝账号',
    wechat_account VARCHAR(50) COMMENT '微信账号',
    total_commission DECIMAL(10,2) DEFAULT 0 COMMENT '累计佣金',
    available_commission DECIMAL(10,2) DEFAULT 0 COMMENT '可提现佣金',
    frozen_commission DECIMAL(10,2) DEFAULT 0 COMMENT '冻结佣金',
    total_withdraw DECIMAL(10,2) DEFAULT 0 COMMENT '累计提现',
    total_fans INT DEFAULT 0 COMMENT '累计粉丝数',
    total_orders INT DEFAULT 0 COMMENT '累计订单数',
    status TINYINT DEFAULT 0 COMMENT '状态 0-待审核 1-已通过 2-已拒绝 3-已禁用',
    apply_time DATETIME COMMENT '申请时间',
    audit_time DATETIME COMMENT '审核时间',
    audit_remark TEXT COMMENT '审核备注',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 分销关系表
CREATE TABLE distribution_relation (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT UNIQUE COMMENT '用户ID',
    parent_id BIGINT COMMENT '上级分销员ID',
    grandparent_id BIGINT DEFAULT 0 COMMENT '上上级分销员ID',
    bind_time DATETIME COMMENT '绑定时间',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_parent_id (parent_id),
    INDEX idx_grandparent_id (grandparent_id)
);

-- 佣金记录表
CREATE TABLE commission_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    distributor_id BIGINT COMMENT '分销员ID',
    order_no VARCHAR(50) COMMENT '订单号',
    user_id BIGINT COMMENT '购买用户ID',
    type TINYINT COMMENT '类型 1-一级佣金 2-二级佣金 3-团队奖励',
    amount DECIMAL(10,2) COMMENT '佣金金额',
    order_amount DECIMAL(10,2) COMMENT '订单金额',
    rate DECIMAL(5,2) COMMENT '佣金比例',
    status TINYINT DEFAULT 0 COMMENT '状态 0-待结算 1-已结算 2-已扣除',
    settle_time DATETIME COMMENT '结算时间',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_distributor_id (distributor_id),
    INDEX idx_order_no (order_no)
);

-- 提现申请表
CREATE TABLE withdraw_apply (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    distributor_id BIGINT COMMENT '分销员ID',
    amount DECIMAL(10,2) COMMENT '提现金额',
    fee DECIMAL(10,2) COMMENT '手续费',
    actual_amount DECIMAL(10,2) COMMENT '实际到账金额',
    pay_type VARCHAR(20) COMMENT '提现方式 alipay/wechat',
    account VARCHAR(50) COMMENT '提现账号',
    real_name VARCHAR(50) COMMENT '真实姓名',
    status TINYINT DEFAULT 0 COMMENT '状态 0-待审核 1-审核通过 2-审核拒绝 3-打款中 4-已完成',
    audit_user_id BIGINT COMMENT '审核人ID',
    audit_time DATETIME COMMENT '审核时间',
    audit_remark TEXT COMMENT '审核备注',
    pay_time DATETIME COMMENT '打款时间',
    transaction_id VARCHAR(50) COMMENT '打款交易号',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_distributor_id (distributor_id),
    INDEX idx_status (status)
);

-- 分销设置表
CREATE TABLE distribution_settings (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    first_level_rate DECIMAL(5,2) DEFAULT 30.00 COMMENT '一级佣金比例(%)',
    second_level_rate DECIMAL(5,2) DEFAULT 10.00 COMMENT '二级佣金比例(%)',
    team_reward_rate DECIMAL(5,2) DEFAULT 5.00 COMMENT '团队奖励比例(%)',
    min_withdraw_amount DECIMAL(10,2) DEFAULT 10.00 COMMENT '最低提现金额',
    withdraw_fee_rate DECIMAL(5,2) DEFAULT 1.00 COMMENT '提现手续费比例(%)',
    min_withdraw_fee DECIMAL(10,2) DEFAULT 1.00 COMMENT '最低提现手续费',
    settle_days INT DEFAULT 7 COMMENT '佣金结算天数',
    auto_audit TINYINT DEFAULT 1 COMMENT '是否自动审核分销员',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 3.11 站内活动通知系统（核心运营）
#### 3.11.1 设计原则
- **精准触达**：根据用户属性和行为推送个性化活动
- **不打扰**：控制推送频率，避免引起用户反感
- **可配置**：所有活动和通知都可在管理后台配置
- **数据驱动**：实时统计活动效果，优化活动策略

#### 3.11.2 核心功能
- **活动管理**：
   - 活动创建：支持创建多种类型的活动
      - 限时折扣：指定时间段内会员套餐打折
      - 买一送一：购买年度VIP送3个月VIP
      - 节日活动：春节、世界睡眠日等主题活动
      - 新用户专享：新用户首次购买优惠
      - 分销活动：限时高佣金、邀请有礼
   - 活动编辑：修改活动信息、时间、规则
   - 活动上下架：手动控制活动的开始和结束
   - 活动优先级：多个活动同时进行时，设置优先级
- **活动展示**：
   - 首页弹窗：APP启动时展示热门活动
   - 首页Banner：轮播展示活动信息
   - 会员页活动专区：集中展示所有会员活动
   - 支付页活动提示：支付时显示可用优惠
   - 个人中心活动入口：查看所有可参与的活动
- **活动通知**：
   - 推送通知：向符合条件的用户推送活动消息
   - 站内信通知：发送活动详情到用户站内信
   - 短信通知：向流失用户和高价值用户发送短信
   - 邮件通知：向绑定邮箱的用户发送邮件
   - AI主动通知：AI聊天时主动推荐合适的活动
- **优惠券管理**：
   - 优惠券创建：创建折扣券、满减券、立减券
   - 优惠券发放：
      - 手动发放：向指定用户发放优惠券
      - 自动发放：新用户注册、完成任务自动发放
      - 活动发放：参与活动获得优惠券
   - 优惠券使用：支付时自动抵扣可用优惠券
   - 优惠券过期提醒：优惠券过期前3天发送提醒
- **活动数据统计**：
   - 活动参与人数：查看活动的总参与人数
   - 活动转化率：查看活动带来的订单转化率
   - 活动收入：查看活动带来的总收入
   - 优惠券使用情况：查看优惠券的领取和使用情况
   - 活动效果对比：对比不同活动的效果

#### 3.11.3 接口设计
```http
# 用户端活动接口
GET  /api/app/activity/list
GET  /api/app/activity/detail/{id}
POST /api/app/activity/join/{id}
GET  /api/app/coupon/list
POST /api/app/coupon/receive/{id}

# 管理后台活动接口
GET  /api/admin/activity/list
POST /api/admin/activity/create
POST /api/admin/activity/update/{id}
POST /api/admin/activity/online/{id}
POST /api/admin/activity/offline/{id}
GET  /api/admin/activity/statistics/{id}

# 管理后台优惠券接口
GET  /api/admin/coupon/list
POST /api/admin/coupon/create
POST /api/admin/coupon/update/{id}
POST /api/admin/coupon/grant/{id}
GET  /api/admin/coupon/statistics/{id}

# 管理后台通知接口
POST /api/admin/notification/push
POST /api/admin/notification/sendSms
POST /api/admin/notification/sendEmail
```

#### 3.11.4 数据表设计
```sql
-- 活动表
CREATE TABLE activity (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(100) COMMENT '活动标题',
    description TEXT COMMENT '活动描述',
    cover VARCHAR(255) COMMENT '活动封面',
    type VARCHAR(20) COMMENT '活动类型 discount/buy_one_get_one/new_user/distribution',
    rule JSON COMMENT '活动规则',
    start_time DATETIME COMMENT '开始时间',
    end_time DATETIME COMMENT '结束时间',
    priority INT DEFAULT 0 COMMENT '优先级',
    status TINYINT DEFAULT 0 COMMENT '状态 0-草稿 1-已上线 2-已下线',
    join_count INT DEFAULT 0 COMMENT '参与人数',
    order_count INT DEFAULT 0 COMMENT '订单数',
    total_amount DECIMAL(10,2) DEFAULT 0 COMMENT '总金额',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 用户活动参与表
CREATE TABLE user_activity (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    activity_id BIGINT COMMENT '活动ID',
    join_time DATETIME COMMENT '参与时间',
    order_no VARCHAR(50) COMMENT '关联订单号',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_activity (user_id, activity_id)
);

-- 推送任务表
CREATE TABLE push_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(100) COMMENT '推送标题',
    content TEXT COMMENT '推送内容',
    target_type VARCHAR(20) COMMENT '目标类型 all/user_group/specified',
    target_ids JSON COMMENT '目标用户ID列表',
    push_time DATETIME COMMENT '推送时间',
    status TINYINT DEFAULT 0 COMMENT '状态 0-待推送 1-推送中 2-已完成 3-已失败',
    success_count INT DEFAULT 0 COMMENT '成功数',
    fail_count INT DEFAULT 0 COMMENT '失败数',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 3.12 白名单系统
#### 3.12.1 核心功能
- **多类型白名单**：用户白名单、设备白名单、IP白名单、VIP白名单
- **白名单管理**：添加、删除、查询、设置过期时间
- **权限控制**：通过注解方式控制接口访问权限
- **灰度发布**：用于新功能内测和灰度发布

#### 3.12.2 接口设计
```http
# 管理后台接口
GET  /api/admin/whitelist/list
POST /api/admin/whitelist/add
POST /api/admin/whitelist/delete/{id}
POST /api/admin/whitelist/updateStatus/{id}
```

#### 3.12.3 数据表设计
```sql
CREATE TABLE whitelist (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    type VARCHAR(20) COMMENT '类型 user/device/ip/vip',
    identifier VARCHAR(100) COMMENT '标识',
    description VARCHAR(255) COMMENT '描述',
    status TINYINT DEFAULT 1 COMMENT '状态',
    expire_time DATETIME COMMENT '过期时间',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 3.13 成长体系
#### 3.13.1 核心功能
- **等级系统**：根据总专注时长和总睡眠时长升级
- **成就系统**：丰富的成就和徽章，覆盖各个使用场景
- **每日任务**：完成任务获得积分
- **积分商城**：积分兑换VIP天数、虚拟商品、优惠券
- **连续打卡**：连续专注/睡眠打卡获得额外奖励

#### 3.13.2 接口设计
```http
GET  /api/app/growth/info
GET  /api/app/achievement/list
GET  /api/app/task/daily
POST /api/app/task/claimReward/{taskId}
GET  /api/app/mall/goods
POST /api/app/mall/exchange/{goodsId}
```

#### 3.13.3 数据表设计
```sql
-- 用户成长表
CREATE TABLE user_growth (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT UNIQUE COMMENT '用户ID',
    level INT DEFAULT 1 COMMENT '等级',
    exp INT DEFAULT 0 COMMENT '经验值',
    points INT DEFAULT 0 COMMENT '积分',
    continuous_focus_days INT DEFAULT 0 COMMENT '连续专注天数',
    continuous_sleep_days INT DEFAULT 0 COMMENT '连续睡眠天数',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 成就表
CREATE TABLE achievement (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) COMMENT '成就名称',
    description VARCHAR(255) COMMENT '成就描述',
    icon VARCHAR(255) COMMENT '图标',
    condition_type VARCHAR(20) COMMENT '条件类型',
    condition_value INT COMMENT '条件值',
    points_reward INT COMMENT '积分奖励',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 用户成就表
CREATE TABLE user_achievement (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    achievement_id BIGINT COMMENT '成就ID',
    obtain_time DATETIME COMMENT '获得时间',
    UNIQUE KEY uk_user_achievement (user_id, achievement_id)
);

-- 每日任务表
CREATE TABLE daily_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) COMMENT '任务名称',
    description VARCHAR(255) COMMENT '任务描述',
    condition_type VARCHAR(20) COMMENT '条件类型',
    condition_value INT COMMENT '条件值',
    points_reward INT COMMENT '积分奖励',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 用户任务表
CREATE TABLE user_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    task_id BIGINT COMMENT '任务ID',
    date DATE COMMENT '日期',
    progress INT DEFAULT 0 COMMENT '进度',
    status TINYINT DEFAULT 0 COMMENT '状态 0-进行中 1-已完成 2-已领取',
    UNIQUE KEY uk_user_task_date (user_id, task_id, date)
);

-- 积分商城商品表
CREATE TABLE mall_goods (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) COMMENT '商品名称',
    description VARCHAR(255) COMMENT '商品描述',
    cover VARCHAR(255) COMMENT '商品封面',
    type VARCHAR(20) COMMENT '类型 vip/coupon/virtual',
    price INT COMMENT '积分价格',
    stock INT COMMENT '库存',
    status TINYINT DEFAULT 1 COMMENT '状态',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 用户兑换记录表
CREATE TABLE user_exchange (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    goods_id BIGINT COMMENT '商品ID',
    points INT COMMENT '消耗积分',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id)
);
```

### 3.14 数据统计模块
#### 3.14.1 核心功能
- **专注统计**：今日、本周、本月专注时长，专注标签分布，最佳专注时间段
- **睡眠统计**：今日、本周、本月睡眠时长，睡眠质量趋势，睡眠阶段分布
- **情绪统计**：情绪变化趋势，情绪类型分布
- **AI使用统计**：AI聊天次数，语音使用次数
- **分销统计**：推广人数、订单数、佣金收入趋势
- **活动统计**：活动参与人数、转化率、收入
- **可视化展示**：折线图、柱状图、热力图

#### 3.14.2 接口设计
```http
GET /api/app/stat/focus?period=week
GET /api/app/stat/sleep?period=month
GET /api/app/stat/emotion?period=week
GET /api/app/stat/ai?period=month
GET /api/app/stat/distribution?period=month
GET /api/app/stat/activity?period=month
```

### 3.15 动态社区模块
#### 3.15.1 设计原则
**安静、温暖、互相鼓励**，避免嘈杂和过度社交。社区的核心目的是**分享进步、互相监督、获得支持**，而不是追求流量和热度。

#### 3.15.2 核心功能
- **动态发布**：
   - 自动生成动态：完成专注、达成成就、连续打卡时自动生成可编辑动态
   - 手动发布动态：支持文字+最多9张图片
   - 动态标签：#专注打卡 #睡眠打卡 #今日心情 等
   - 匿名发布：保护用户隐私
   - AI文案生成：辅助生成优美的动态文案
- **动态浏览**：
   - 关注流：查看关注用户的动态
   - 推荐流：算法推荐优质正能量内容
   - 话题广场：按话题分类浏览
   - 我的动态：查看自己发布的所有内容
- **互动功能**：
   - 点赞、评论、回复、分享、收藏
   - 举报违规内容、屏蔽不喜欢的用户
- **好友系统**：
   - 关注/取消关注、粉丝/关注列表
   - 好友推荐（基于兴趣和地理位置）
   - 私信聊天（VIP专属）
- **排行榜**：
   - 专注时长榜（日/周/月）
   - 连续打卡榜
   - 睡眠质量榜
   - 好友专属排行榜
- **官方挑战活动**：
   - 21天专注挑战、早睡早起30天挑战等
   - 系统自动记录进度，完成获得积分/VIP/专属徽章
   - 挑战进度自动同步到动态

#### 3.15.3 接口设计
```http
# 动态管理
POST /api/app/moment/publish
GET  /api/app/moment/list
GET  /api/app/moment/detail/{id}
POST /api/app/moment/delete/{id}

# 互动功能
POST /api/app/moment/like/{id}
POST /api/app/moment/unlike/{id}
POST /api/app/moment/comment/{id}
POST /api/app/moment/deleteComment/{id}
POST /api/app/moment/share/{id}
POST /api/app/moment/collect/{id}

# 好友系统
POST /api/app/user/follow/{userId}
POST /api/app/user/unfollow/{userId}
GET  /api/app/user/followers/{userId}
GET  /api/app/user/following/{userId}
POST /api/app/message/send
GET  /api/app/message/list

# 排行榜
GET  /api/app/rank/focus?period=week
GET  /api/app/rank/sleep?period=month
GET  /api/app/rank/achievement

# 挑战活动
GET  /api/app/challenge/list
POST /api/app/challenge/join/{id}
GET  /api/app/challenge/my
GET  /api/app/challenge/progress/{id}
```

#### 3.15.4 数据表设计
```sql
-- 动态表
CREATE TABLE moment (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    content TEXT COMMENT '内容',
    images JSON COMMENT '图片URL列表',
    type VARCHAR(20) COMMENT '类型 auto/manual',
    source VARCHAR(20) COMMENT '来源 focus/sleep/achievement',
    source_id BIGINT COMMENT '来源ID',
    is_anonymous TINYINT DEFAULT 0 COMMENT '是否匿名',
    visibility TINYINT DEFAULT 0 COMMENT '可见范围 0-公开 1-仅好友 2-仅自己',
    like_count INT DEFAULT 0 COMMENT '点赞数',
    comment_count INT DEFAULT 0 COMMENT '评论数',
    share_count INT DEFAULT 0 COMMENT '分享数',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    is_deleted TINYINT DEFAULT 0,
    INDEX idx_user_id (user_id),
    INDEX idx_create_time (create_time)
);

-- 动态标签表
CREATE TABLE moment_tag (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    moment_id BIGINT COMMENT '动态ID',
    tag VARCHAR(50) COMMENT '标签',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_tag (tag)
);

-- 点赞表
CREATE TABLE moment_like (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    moment_id BIGINT COMMENT '动态ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_moment (user_id, moment_id)
);

-- 评论表
CREATE TABLE moment_comment (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    moment_id BIGINT COMMENT '动态ID',
    parent_id BIGINT DEFAULT 0 COMMENT '父评论ID',
    content TEXT COMMENT '内容',
    like_count INT DEFAULT 0 COMMENT '点赞数',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    is_deleted TINYINT DEFAULT 0,
    INDEX idx_moment_id (moment_id)
);

-- 关注表
CREATE TABLE user_follow (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    follower_id BIGINT COMMENT '关注者ID',
    following_id BIGINT COMMENT '被关注者ID',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_follower_following (follower_id, following_id)
);

-- 私信表
CREATE TABLE private_message (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    from_user_id BIGINT COMMENT '发送者ID',
    to_user_id BIGINT COMMENT '接收者ID',
    content TEXT COMMENT '内容',
    is_read TINYINT DEFAULT 0 COMMENT '是否已读',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_to_user_id (to_user_id)
);

-- 挑战活动表
CREATE TABLE challenge (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(100) COMMENT '标题',
    description TEXT COMMENT '描述',
    cover VARCHAR(255) COMMENT '封面',
    type VARCHAR(20) COMMENT '类型 focus/sleep',
    duration INT COMMENT '持续天数',
    condition_value INT COMMENT '每日目标值',
    points_reward INT COMMENT '积分奖励',
    vip_days_reward INT COMMENT 'VIP天数奖励',
    badge_id BIGINT COMMENT '专属徽章ID',
    start_time DATETIME COMMENT '开始时间',
    end_time DATETIME COMMENT '结束时间',
    status TINYINT DEFAULT 1 COMMENT '状态',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 用户挑战表
CREATE TABLE user_challenge (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    challenge_id BIGINT COMMENT '挑战ID',
    join_time DATETIME COMMENT '加入时间',
    current_day INT DEFAULT 0 COMMENT '当前天数',
    completed_days INT DEFAULT 0 COMMENT '完成天数',
    status TINYINT DEFAULT 0 COMMENT '状态 0-进行中 1-已完成 2-已失败',
    UNIQUE KEY uk_user_challenge (user_id, challenge_id)
);
```

### 3.16 通知消息模块
#### 3.16.1 核心功能
- **系统通知**：会员到期提醒、成就获得通知、挑战进度通知、系统公告
- **互动通知**：点赞、评论、关注、私信通知
- **提醒通知**：专注提醒、睡眠提醒、喝水提醒、任务提醒
- **分销通知**：新粉丝通知、新订单通知、佣金到账通知、提现到账通知
- **活动通知**：活动开始提醒、优惠券到账提醒、优惠券过期提醒
- **支付通知**：支付成功通知、自动续费提醒、退款成功通知
- **消息管理**：标记已读、批量已读、删除消息、清空消息
- **通知设置**：自定义各类通知的开关和提醒方式（震动/声音）

#### 3.16.2 接口设计
```http
GET  /api/app/notification/list
POST /api/app/notification/markRead/{id}
POST /api/app/notification/markAllRead
POST /api/app/notification/delete/{id}
GET  /api/app/notification/settings
POST /api/app/notification/settings/update
```

#### 3.16.3 数据表设计
```sql
CREATE TABLE notification (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT COMMENT '用户ID',
    type VARCHAR(20) COMMENT '类型 system/interaction/reminder/distribution/activity/pay',
    title VARCHAR(100) COMMENT '标题',
    content TEXT COMMENT '内容',
    target_type VARCHAR(20) COMMENT '目标类型 moment/user/challenge/order/activity',
    target_id BIGINT COMMENT '目标ID',
    is_read TINYINT DEFAULT 0 COMMENT '是否已读',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_is_read (is_read),
    INDEX idx_type (type)
);
```

### 3.17 管理后台
#### 3.17.1 用户管理
- 用户列表、用户详情、用户数据导出
- 用户封禁/解封
- 会员手动开通/取消、时长调整
- 用户数据异常排查

#### 3.17.2 内容管理
- 音频上传、编辑、上下架、分类管理
- 官方混音包管理
- Banner管理、首页文案管理
- 动态内容审核、违规内容处理
- 敏感词库管理

#### 3.17.3 AI管理
- Prompt模板管理、多场景Prompt配置
- AI人格配置、语气调整
- AI语音管理、上下架
- AI接口调用统计、限流配置

#### 3.17.4 订单管理
- 全量订单列表与多条件筛选
- 订单详情查看（包含分销佣金明细）
- 订单取消与退款审核
- 订单导出与统计分析
- 退款记录管理

#### 3.17.5 支付管理
- 支付渠道配置
- 交易记录查询
- 退款审核与处理
- 自动续费管理
- 支付数据统计

#### 3.17.6 活动管理
- 活动创建与管理
- 优惠券创建与发放
- 推送任务管理
- 活动数据统计
- 短信/邮件发送记录

#### 3.17.7 分销管理
- 分销员列表与审核
- 分销员详情与数据统计
- 分销订单列表
- 佣金记录管理
- 提现申请审核与打款
- 分销规则配置
- 分销数据统计

#### 3.17.8 运营管理
- 会员套餐管理、价格调整
- 白名单管理、灰度发布
- 成就与任务管理
- 挑战活动创建与管理
- 推送通知管理

#### 3.17.9 数据分析
- 用户增长统计、DAU/MAU/留存分析
- 功能使用统计、核心行为漏斗分析
- 会员转化统计、收入分析
- AI使用统计、接口性能分析
- 分销数据统计、推广效果分析
- 活动数据统计、活动效果分析

---

## 四、UI设计规范
### 4.1 核心原则
**情绪氛围优先于功能堆砌**，打造安静、舒适、沉浸式的使用体验。

### 4.2 设计要求
| 项目 | 要求 |
|------|------|
| 配色 | 深色主题为主，使用低饱和度渐变，避免刺眼颜色 |
| 动效 | 缓慢、柔和、有呼吸感，避免快速闪烁和剧烈动画 |
| 留白 | 大量留白，减少视觉干扰 |
| 背景 | 动态渐变背景，根据时间和场景变化 |
| 卡片 | 半透明磨砂玻璃效果，圆角16px |
| 字体 | 无衬线字体，字号适中，行高1.5 |
| 节奏 | 页面切换缓慢，操作反馈柔和 |

### 4.3 核心页面设计
- **首页**：全屏动态背景，中央显示问候语和快速入口，底部导航栏
- **专注页**：全屏倒计时，呼吸动画，底部控制按钮，无其他干扰元素
- **睡眠页**：深色背景，柔和的星星动画，助眠音列表，唤醒设置
- **AI聊天页**：气泡式聊天，打字机动效，语音输入按钮
- **动态页**：卡片式布局，简洁清爽，突出内容本身
- **个人主页**：展示用户基本信息、等级、成就、统计数据和动态
- **会员页**：清晰的权益对比，突出年度VIP和终身VIP的价值
- **分销页**：简洁的数据展示，醒目的推广按钮，清晰的佣金规则
- **活动页**：突出活动主题和优惠信息，简洁明了的参与按钮

---

## 五、开发计划与优先级
### 第一阶段（基础功能，4周）
- 用户系统（登录、注册、个人信息、账号安全）
- 白噪音模块（播放、列表、基础混音）
- 专注模块（番茄钟、计时、基础记录）
- 基础数据统计
- 管理后台基础功能

### 第二阶段（核心体验，6周）
- 深度专注模式（APP锁、系统勿扰、分心检测）
- 完整混合音频引擎（3-5种音频混合、自定义保存）
- 睡眠模块（助眠音、定时关闭、基础记录）
- AI聊天基础版（非流式响应、多场景Prompt）
- 成长体系（等级、成就、每日任务）
- 会员系统基础版（权限控制、权益展示）

### 第三阶段（差异化与变现，8周）
- AI流式响应与主动交互系统
- 智能睡眠监测与智能唤醒
- AI语音合成与语音输入
- **完整支付系统**（多渠道支付、自动续费、退款）
- **完整订单列表与退款系统**
- 高级数据统计与报告
- 白名单与灰度发布系统
- 桌面小组件与通知栏快捷操作
- 基础动态社区功能（发布、浏览、点赞、评论）
- 通知消息模块

### 第四阶段（增长与生态，6周）
- **站内活动通知系统**（活动管理、优惠券、推送）
- **二级分销裂变系统**
- 完整社区功能（好友、私信、排行榜、挑战活动）
- 社交分享功能优化
- 运营活动系统
- 性能优化与bug修复

### 第五阶段（扩展功能，长期）
- AI虚拟形象
- 多人专注房间
- 智能手表支持
- 智能家居集成
- 企业版
- 国际化

---

## 六、技术实现要点
### 6.1 移动端音频播放
- 使用Howler.js，强制开启html5模式以支持锁屏控制
- 处理iOS Safari自动播放限制，在用户交互事件中初始化音频上下文
- 实现音频淡入淡出效果，避免突兀
- 处理系统中断（电话、闹钟）后的自动恢复
- 使用Service Worker实现音频离线缓存
- 支持音频混合播放，独立控制每种音频的音量

### 6.2 AI模块实现
- 使用Spring AI + LangChain4j集成通义千问和DeepSeek
- 实现SSE流式响应，提供打字机效果
- 使用Redis存储最近20轮对话上下文
- 设计多场景Prompt模板，配置化管理
- 实现AI接口限流与降级，异常时返回预设回复库
- 异步处理AI语音合成任务，使用RabbitMQ队列

### 6.3 订单与支付实现
- 使用分布式锁防止重复下单
- 实现订单15分钟自动取消机制
- 集成支付宝、微信支付、Apple Pay官方SDK
- 实现支付结果异步通知与主动查询双重校验
- 订单状态流转严格控制，防止状态异常
- 退款流程与第三方支付平台对接
- 实现优惠券和积分抵扣逻辑

### 6.4 活动系统实现
- 活动规则配置化，支持多种活动类型
- 优惠券生成、发放、使用、过期全流程管理
- 推送任务定时执行，支持定时推送
- 活动数据实时统计与展示
- 活动优先级控制，多个活动同时进行时自动选择最优优惠

### 6.5 分销系统实现
- 推广链接生成与解析，自动绑定上下级关系
- 防作弊机制：设备指纹、IP限制、异常行为检测
- 佣金计算与结算：订单支付后立即计算，定时任务自动结算
- 提现流程：自动审核小额提现，大额提现人工审核
- 分销数据实时统计与排行榜更新
- 严格遵守二级分销合规要求

### 6.6 社区模块实现
- 实现内容审核机制，结合敏感词过滤和人工审核
- 设计简单高效的推荐算法，优先展示优质正能量内容
- 使用Redis缓存热门动态和排行榜数据
- 实现动态发布的图片压缩和上传
- 私信功能使用WebSocket实现实时消息推送

### 6.7 后端性能优化
- 使用Redis缓存用户信息、音频列表、会员信息等热点数据
- 使用分布式锁防止重复下单、重复打卡
- 使用RabbitMQ异步处理统计计算、AI语音生成等耗时任务
- 所有写操作接口增加幂等性保证
- 数据库读写分离，慢查询优化
- 接口请求频率限制，防止恶意攻击

### 6.8 安全与合规
- 敏感数据加密存储，接口返回脱敏
- 严格的接口权限控制，防止越权访问
- 符合《个人信息保护法》要求，提供账号注销和数据导出功能
- AI聊天内容进行敏感词过滤
- 用户隐私数据本地优先存储，云端加密备份
- 分销系统严格遵守《禁止传销条例》

---

## 七、商业变现策略
### 7.1 核心变现方式
- **订阅制**：月度、年度、终身VIP会员（主要收入来源）
- **活动营销**：通过限时折扣、节日活动提升转化率
- **分销裂变**：通过分销员推广获得新用户和订单
- **虚拟商品**：AI语音包、专属主题、高级音频库、动态置顶
- **广告变现**：免费用户观看广告获得VIP时长
- **企业版**：针对企业用户的团队专注解决方案
- **周边产品**：联名眼罩、香薰、助眠灯等实物产品

### 7.2 转化优化策略
- **自然引导**：在用户使用高级功能时自然引导开通会员
- **免费试用**：新用户7天免费试用，降低决策门槛
- **活动营销**：定期推出限时折扣、买一送一等活动
- **优惠券体系**：新用户注册送优惠券，流失用户送召回优惠券
- **社交裂变**：邀请好友注册双方获得VIP天数和优惠券
- **流失召回**：对流失用户发送专属优惠券和召回短信
- **社区激励**：优质内容创作者获得VIP奖励和优惠券

### 7.3 分销增长策略
1. **种子用户招募**：邀请核心用户成为首批分销员，给予额外奖励
2. **阶梯式奖励**：设置推广目标，达到目标获得额外佣金或VIP奖励
3. **节日活动**：节假日推出限时高佣金活动（如佣金翻倍）
4. **排行榜激励**：每月评选Top10分销员，给予现金奖励和专属徽章
5. **团队激励**：鼓励分销员发展团队，获得团队奖励

---

## 八、运维与监控
### 8.1 系统监控
- 使用Prometheus + Grafana监控系统性能指标
- 使用ELK栈收集和分析日志
- 关键错误通过钉钉/企业微信告警
- 定期进行性能分析和优化
- 监控AI接口调用量和成本
- 监控分销佣金支出和ROI
- 监控活动效果和转化率

### 8.2 数据备份
- 数据库每日全量备份，每小时增量备份
- MinIO对象存储定期备份
- 备份数据异地存储
- 定期进行数据恢复演练

### 8.3 灰度发布
- 使用白名单系统进行新功能灰度发布
- 先开放给内部用户和测试用户
- 逐步扩大灰度范围，观察系统稳定性和用户反馈
- 出现问题及时回滚

---

## 九、风险与应对
| 风险 | 应对措施 |
|------|----------|
| AI接口成本过高 | 实现多级缓存，限制免费用户使用次数，优化Prompt减少token消耗 |
| 移动端兼容性问题 | 针对主流设备和浏览器进行测试，使用成熟的第三方库 |
| 用户留存率低 | 优化首次使用体验，完善成长体系，增加AI主动交互和社区功能 |
| 支付安全问题 | 使用官方支付SDK，严格校验订单信息，实现订单状态异步通知，全程加密传输 |
| 支付渠道故障 | 多支付渠道备份，支付失败时自动切换到备用渠道 |
| 数据安全问题 | 敏感数据加密存储，定期安全审计，防止数据泄露 |
| 社区内容风险 | 建立完善的内容审核机制，结合机器审核和人工审核，及时处理违规内容 |
| 分销合规风险 | 严格采用二级分销模式，明确禁止多级分销；在分销协议中明确合规条款；定期自查分销数据 |
| 佣金成本过高 | 合理设置佣金比例，根据盈利情况动态调整；设置最高佣金上限；对大额订单进行佣金封顶 |
| 作弊刷单风险 | 建立完善的防作弊机制，检测同一设备/IP/手机号的异常注册和下单；对可疑订单进行人工审核；发现作弊行为立即冻结账户和佣金 |
| 提现欺诈风险 | 要求分销员实名认证，绑定本人银行卡/支付宝；提现金额较大时进行人工审核；建立黑名单制度 |
| 活动效果不佳 | 提前进行小范围测试，根据数据反馈优化活动规则；实时监控活动数据，及时调整活动策略 |
| 用户投诉过多 | 建立完善的客服体系，及时处理用户投诉；明确活动规则，避免产生歧义 |

---

## 十、总结
本需求文档是经过多次迭代优化后的终极完整版，完整覆盖了AI沉浸式专注助眠APP从基础功能到核心差异化、从商业变现到用户增长、从技术实现到运维监控的所有方面。文档结构清晰，逻辑严谨，可直接作为开发和运营的指导依据。

在开发过程中，**请始终牢记产品的核心价值：为用户提供沉浸式的专注和助眠体验，以及有温度的AI情绪陪伴和社区支持**。技术是为体验服务的，不要为了追求技术而牺牲用户体验。同时，要始终保持社区的安静、温暖、正能量氛围，避免过度社交和嘈杂。

支付系统和活动系统的加入，彻底完善了产品的商业闭环，为产品的可持续发展提供了坚实的基础。分销系统则为产品提供了强大的用户增长引擎，通过激励老用户推广新用户，可以实现产品的病毒式传播。

建议采用敏捷开发模式，按照优先级逐步实现各个模块，先完成核心的支付和订单系统，再实现活动和运营功能，确保产品能够快速上线并产生收入。同时，要密切关注用户反馈，根据用户需求持续迭代优化产品。

需要我帮你把所有数据表整理成一份**可直接执行的完整SQL建表脚本**吗？
