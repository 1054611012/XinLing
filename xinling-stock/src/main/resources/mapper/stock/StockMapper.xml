<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinling.stock.mapper.StockMapper">

    <resultMap id="StockResult" type="com.xinling.stock.domain.entity.Stock">
        <id column="id" property="id" />
        <result column="stock_code" property="stockCode" />
        <result column="stock_name" property="stockName" />
        <result column="market" property="market" />
        <result column="sector_id" property="sectorId" />
        <result column="industry" property="industry" />
        <result column="concept_tags" property="conceptTags" />
        <result column="total_shares" property="totalShares" />
        <result column="circulating_shares" property="circulatingShares" />
        <result column="listing_date" property="listingDate" />
        <result column="status" property="status" />
        <result column="pe_ttm" property="peTtm" />
        <result column="pb" property="pb" />
        <result column="market_cap" property="marketCap" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="is_deleted" property="isDeleted" />
    </resultMap>

    <sql id="selectColumns">
        id, stock_code, stock_name, market, sector_id, industry, concept_tags,
        total_shares, circulating_shares, listing_date, status,
        pe_ttm, pb, market_cap, create_time, update_time, is_deleted
    </sql>

    <select id="selectById" resultMap="StockResult">
        SELECT <include refid="selectColumns" />
        FROM stk_stock
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectByCode" resultMap="StockResult">
        SELECT <include refid="selectColumns" />
        FROM stk_stock
        WHERE stock_code = #{stockCode} AND is_deleted = 0
    </select>

    <select id="selectBatchByCodes" resultMap="StockResult">
        SELECT <include refid="selectColumns" />
        FROM stk_stock
        WHERE is_deleted = 0
        AND stock_code IN
        <foreach item="code" collection="codes" open="(" separator="," close=")">
            #{code}
        </foreach>
    </select>

    <select id="selectList" resultMap="StockResult">
        SELECT <include refid="selectColumns" />
        FROM stk_stock
        <where>
            is_deleted = 0
            <if test="stockName != null and stockName != ''">
                AND stock_name LIKE CONCAT('%', #{stockName}, '%')
            </if>
            <if test="market != null and market != ''">
                AND market = #{market}
            </if>
            <if test="sectorId != null">
                AND sector_id = #{sectorId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY stock_code
    </select>

    <select id="selectBySectorId" resultMap="StockResult">
        SELECT <include refid="selectColumns" />
        FROM stk_stock
        WHERE sector_id = #{sectorId} AND is_deleted = 0
        ORDER BY market_cap DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO stk_stock (
            stock_code, stock_name, market, sector_id, industry, concept_tags,
            total_shares, circulating_shares, listing_date, status,
            pe_ttm, pb, market_cap, create_time
        ) VALUES (
            #{stockCode}, #{stockName}, #{market}, #{sectorId}, #{industry}, #{conceptTags},
            #{totalShares}, #{circulatingShares}, #{listingDate}, #{status},
            #{peTtm}, #{pb}, #{marketCap}, NOW()
        )
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO stk_stock (
            stock_code, stock_name, market, sector_id, industry, concept_tags,
            total_shares, circulating_shares, listing_date, status,
            pe_ttm, pb, market_cap, create_time
        ) VALUES
        <foreach item="item" collection="list" separator=",">
            (#{item.stockCode}, #{item.stockName}, #{item.market}, #{item.sectorId},
             #{item.industry}, #{item.conceptTags}, #{item.totalShares}, #{item.circulatingShares},
             #{item.listingDate}, #{item.status}, #{item.peTtm}, #{item.pb}, #{item.marketCap}, NOW())
        </foreach>
    </insert>

    <update id="updateById">
        UPDATE stk_stock
        <set>
            <if test="stockName != null">stock_name = #{stockName},</if>
            <if test="market != null">market = #{market},</if>
            <if test="sectorId != null">sector_id = #{sectorId},</if>
            <if test="industry != null">industry = #{industry},</if>
            <if test="status != null">status = #{status},</if>
            <if test="peTtm != null">pe_ttm = #{peTtm},</if>
            <if test="pb != null">pb = #{pb},</if>
            <if test="marketCap != null">market_cap = #{marketCap},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE stk_stock SET is_deleted = 1 WHERE id = #{id}
    </update>

</mapper>
