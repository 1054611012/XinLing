<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinling.stock.mapper.SectorStockMapper">

    <resultMap id="SectorStockResult" type="com.xinling.stock.domain.entity.SectorStock">
        <id column="id" property="id" />
        <result column="sector_id" property="sectorId" />
        <result column="stock_code" property="stockCode" />
        <result column="stock_name" property="stockName" />
        <result column="weight" property="weight" />
        <result column="is_leader" property="isLeader" />
        <result column="create_time" property="createTime" />
        <result column="is_deleted" property="isDeleted" />
    </resultMap>

    <sql id="selectColumns">
        id, sector_id, stock_code, stock_name, weight, is_leader, create_time, is_deleted
    </sql>

    <select id="selectBySectorId" resultMap="SectorStockResult">
        SELECT <include refid="selectColumns" /> FROM stk_sector_stock
        WHERE sector_id = #{sectorId} AND is_deleted = 0
        ORDER BY weight DESC
    </select>

    <select id="selectByStockCode" resultMap="SectorStockResult">
        SELECT <include refid="selectColumns" /> FROM stk_sector_stock
        WHERE stock_code = #{stockCode} AND is_deleted = 0
    </select>

    <select id="selectLeadersBySectorId" resultMap="SectorStockResult">
        SELECT <include refid="selectColumns" /> FROM stk_sector_stock
        WHERE sector_id = #{sectorId} AND is_leader = 1 AND is_deleted = 0
        ORDER BY weight DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO stk_sector_stock (sector_id, stock_code, stock_name, weight, is_leader, create_time)
        VALUES (#{sectorId}, #{stockCode}, #{stockName}, #{weight}, #{isLeader}, NOW())
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO stk_sector_stock (sector_id, stock_code, stock_name, weight, is_leader, create_time)
        VALUES
        <foreach item="item" collection="list" separator=",">
            (#{item.sectorId}, #{item.stockCode}, #{item.stockName}, #{item.weight}, #{item.isLeader}, NOW())
        </foreach>
    </insert>

    <delete id="deleteBySectorId">
        UPDATE stk_sector_stock SET is_deleted = 1 WHERE sector_id = #{sectorId}
    </delete>

</mapper>
